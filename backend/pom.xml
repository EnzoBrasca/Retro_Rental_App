<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.retrorental</groupId>
	<artifactId>backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.5</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.5</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
    		<groupId>io.github.cdimascio</groupId>
    		<artifactId>dotenv-java</artifactId>
    		<version>3.0.0</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.5</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.minio</groupId>
			<artifactId>minio</artifactId>
			<version>8.5.17</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!-- Migraciones de schema.
		     spring-boot-flyway trae la AUTOCONFIGURACION: en Spring Boot 4 las
		     autoconfiguraciones se separaron en un modulo por tecnologia, asi que
		     flyway-core por si solo pone la libreria en el classpath pero nadie la
		     ejecuta al arrancar. Este modulo arrastra flyway-core transitivamente.
		     flyway-database-postgresql es aparte porque desde Flyway 10 el soporte
		     de cada motor vive en su propio artefacto. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Postgres efimero en Docker para el test de contexto.
		     BackendApplicationTests levanta el contexto COMPLETO: corre las
		     migraciones Flyway de verdad y deja que Hibernate valide las
		     entidades contra el schema resultante (ddl-auto: validate). Es lo
		     que avisa si alguien agrega un campo a una entidad y se olvida la
		     migracion; sin esto, eso recien se descubre cuando la app no
		     arranca en produccion. Requiere Docker corriendo. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Testcontainers 2.x renombro los modulos: el de Postgres es
		     testcontainers-postgresql, no postgresql. La version la gestiona el
		     parent de Spring Boot (testcontainers.version). -->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
					<compilerArgs>
						<arg>-proc:full</arg>
					</compilerArgs>
				</configuration>
			</plugin>
			<!--
			  Cobertura. Antes no habia forma de saber si los agujeros de tests se
			  estaban cerrando o agrandando: sin medicion no hay mejora, hay opinion.

			  El reporte se genera en cada `mvn test` (target/site/jacoco/index.html)
			  y NO rompe el build por umbral. Es deliberado: un umbral duro puesto de
			  entrada sobre un proyecto con agujeros conocidos obliga a elegir entre
			  bajarlo hasta que no signifique nada o bloquear a todo el mundo. Primero
			  se establece la linea de base; el umbral se sube despues, a conciencia.
			-->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<!--
				  Version FIJADA. El parent de Spring Boot no gestiona este plugin, asi
				  que sin esto Maven resuelve la ultima publicada y el build deja de ser
				  reproducible: la misma cabeza de git puede medir distinto la semana que
				  viene.
				-->
				<version>0.8.15</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!--
		  Chequeo de CVEs en las dependencias. Va en un perfil OPT-IN y no en el
		  build de siempre a proposito: dependency-check descarga la base del NVD,
		  que tarda varios minutos la primera vez y desde 2023 pide API key para no
		  comerse el rate limit. Atado a `verify` dejaria a cualquiera que clone el
		  repo con un build lento o roto por un motivo que no tiene que ver con su
		  cambio.

		  Uso:  ./mvnw verify -Pseguridad -DnvdApiKey=<key>
		  Pedir la key gratis en https://nvd.nist.gov/developers/request-an-api-key

		  Importa aca porque esta app maneja auth, JWT, subida de archivos y rate
		  limiting, y arrastra tres dependencias con version fijada a mano (jjwt,
		  minio, dotenv-java) que el BOM de Spring Boot NO actualiza solo.
		-->
		<profile>
			<id>seguridad</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.owasp</groupId>
						<artifactId>dependency-check-maven</artifactId>
						<version>10.0.4</version>
						<configuration>
							<!-- Falla el build ante cualquier CVE de severidad alta o mas. -->
							<failBuildOnCVSS>7</failBuildOnCVSS>
							<nvdApiKey>${nvdApiKey}</nvdApiKey>
						</configuration>
						<executions>
							<execution>
								<goals>
									<goal>check</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
